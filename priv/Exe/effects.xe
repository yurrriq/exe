
    import Eff
    
    data IO: (A:*) → * :=
         (in: (String → IO) → IO)
         (out: String → IO → IO)
         (rnd: (nat → IO) → IO)
         (raise: String → IO → IO)
         (term: IO)
